import React from "react"
import Subscription from "./subscription"

export default (props) => (
    <Subscription btnTitle={props.btnTitle} btnClass={props.btnClass} inputPlacentholder={props.inputPlacentholde}/>
)